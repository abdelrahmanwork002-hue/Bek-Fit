
  # Bek Fit

  This is a code bundle for Bek Fit. The original project is available at https://www.figma.com/design/EexQ7aJHaEjP5yuLIcSNp1/Bek-Fit.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  